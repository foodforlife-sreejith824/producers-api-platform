apiVersion: v1
kind: Service
metadata:
  name: {{ include "foodforlife-producers-api.fullname" . }}
  labels:
    {{- include "foodforlife-producers-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foodforlife-producers-api.selectorLabels" . | nindent 4 }}
