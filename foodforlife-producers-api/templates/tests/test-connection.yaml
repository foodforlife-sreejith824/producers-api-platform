apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foodforlife-producers-api.fullname" . }}-test-connection"
  labels:
    {{- include "foodforlife-producers-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "foodforlife-producers-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
